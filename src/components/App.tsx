import React from 'react';
import { AppBackground, AppSubtitle, AppDisplay, AppButton } from './App.styles'


function App() {
  return (
    <AppBackground>
      <AppSubtitle>Create your own</AppSubtitle>
      <AppDisplay>NFT Collection</AppDisplay>
      <AppButton>Get Started</AppButton>
    </AppBackground>
  );
}

export default App;
